<UserControl x:Class="ActuarialModelApp.UI.ClaimsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Register Claim"
                Width="140"
                Margin="0,0,0,10"
                Click="RegisterClaim_Click"/>

        <DataGrid x:Name="ClaimsGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="True"
                  IsReadOnly="True"/>
    </Grid>
</UserControl>
